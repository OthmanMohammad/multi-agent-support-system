# =============================================================================
# FRONTEND ENVIRONMENT VARIABLES
# =============================================================================
#
# Copy this file to .env.local and fill in the values
# Never commit .env.local to version control!
#
# IMPORTANT FOR PRODUCTION:
# - NEXT_PUBLIC_* variables are embedded at BUILD TIME
# - You must set these variables BEFORE building the app
# - If you change these values, you need to rebuild the app
#
# Common issues when these are not set correctly:
# - "Infinite loading" on sign-in/dashboard: API URL pointing to localhost
# - "Session loading forever": Missing or invalid NEXTAUTH_SECRET
# - OAuth not working: NEXTAUTH_URL not matching your domain

# =============================================================================
# API CONFIGURATION (REQUIRED)
# =============================================================================

# Backend API URL - MUST be set correctly for production!
# Development: http://localhost:8000
# Production: https://api.yourdomain.com or http://your-server-ip:8000
#
# IMPORTANT: This is embedded at build time! If left as localhost in production,
# the frontend will try to call http://localhost:8000 from the user's browser,
# which will fail. Set this to your actual backend URL before building.
NEXT_PUBLIC_API_URL=http://localhost:8000

# =============================================================================
# NEXTAUTH CONFIGURATION (REQUIRED)
# =============================================================================

# NextAuth URL - MUST match your frontend URL exactly!
# Development: http://localhost:3000
# Production: https://yourdomain.com (no trailing slash)
#
# IMPORTANT: This must match the URL users access your site from.
# Mismatched URLs will cause session/cookie issues.
NEXTAUTH_URL=http://localhost:3000

# NextAuth Secret - REQUIRED for production!
# Generate with: openssl rand -base64 32
# MUST be at least 32 characters
#
# IMPORTANT: Without a valid secret, NextAuth sessions will fail to load,
# causing infinite loading screens. Generate a unique secret for production.
NEXTAUTH_SECRET=your-secret-key-min-32-chars-change-this-in-production

# =============================================================================
# OAUTH (Optional - for Google/GitHub login)
# =============================================================================

# Google OAuth
# Get from: https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: https://yourdomain.com/api/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# GitHub OAuth
# Get from: https://github.com/settings/developers
# Authorization callback URL: https://yourdomain.com/api/auth/callback/github
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# =============================================================================
# FEATURE FLAGS
# =============================================================================

# Enable Mock Service Worker (for development with fake data)
# Set to "false" for production to use real backend
NEXT_PUBLIC_ENABLE_MSW=false

# =============================================================================
# ENVIRONMENT
# =============================================================================

# Node environment (automatically set by Next.js)
# NODE_ENV=development

# =============================================================================
# PRODUCTION CHECKLIST
# =============================================================================
#
# Before deploying to production:
# [ ] Set NEXT_PUBLIC_API_URL to your backend URL (e.g., https://api.yourdomain.com)
# [ ] Set NEXTAUTH_URL to your frontend URL (e.g., https://yourdomain.com)
# [ ] Generate a secure NEXTAUTH_SECRET: openssl rand -base64 32
# [ ] Configure OAuth providers if using social login
# [ ] Rebuild the app after setting these variables
# [ ] Test sign-in/sign-up flow after deployment
